# =============================================================================
# AML Hybrid System — Python Dependencies
# =============================================================================
# QUICK INSTALL (CPU-only dev):
#   pip install -r requirements.txt
#
# FULL GPU (CUDA 12.1):
#   pip install torch==2.3.0 --index-url https://download.pytorch.org/whl/cu121
#   pip install torch-geometric
#   pip install pyg-lib torch-scatter torch-sparse \
#       -f https://data.pyg.org/whl/torch-2.3.0+cu121.html
#   pip install -r requirements.txt
# =============================================================================

# ── Graph DB & Vector Store ──────────────────────────────────────────────────
neo4j>=5.18.0
pymilvus>=2.4.3

# ── LangGraph / LangChain ────────────────────────────────────────────────────
langgraph>=1.1.3
langgraph-checkpoint>=4.0.1
langchain>=1.2.0
langchain-core>=1.2.0
langchain-community>=0.4.0

# ── LLM / NIM ────────────────────────────────────────────────────────────────
openai>=1.30.0
httpx>=0.27.0

# ── GNN / ML ─────────────────────────────────────────────────────────────────
# torch>=2.3.0         # install separately with CUDA wheel
# torch-geometric>=2.5.0
polars>=0.20.0
numpy>=1.26.0
scipy>=1.13.0
scikit-learn>=1.4.0

# ── Embeddings ───────────────────────────────────────────────────────────────
sentence-transformers>=3.0.0
transformers>=4.40.0

# ── API / Async ───────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.10.0
pydantic-settings>=2.13.0

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
tenacity>=8.3.0
rich>=13.7.0
structlog>=24.1.0
tqdm>=4.66.0

# ── Dev / Test ───────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
